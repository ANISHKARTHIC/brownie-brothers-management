import type { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'com.browniebrothers.app',
  appName: 'Brownie Brothers',
  webDir: 'dist'
};

export default config;
